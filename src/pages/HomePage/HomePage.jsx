import React from "react";
import QuestionInput from "../../components/QuestionInput/QuestionInput";

const HomePage = ({ onSubmit }) => {
  return <QuestionInput onSubmit={onSubmit} />;
};

export default HomePage;
